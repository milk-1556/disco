{
  "name": "@disco/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "hash-password": "tsx src/scripts/hashPassword.ts",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@disco/core": "workspace:*",
    "@disco/schema": "workspace:*",
    "@disco/sdk": "workspace:*",
    "@fastify/cors": "^10.0.1",
    "bcryptjs": "^2.4.3",
    "fastify": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/jsonwebtoken": "^9.0.7"
  }
}
